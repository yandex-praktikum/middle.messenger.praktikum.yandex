<div class='input'>
    <label for={{name}}>{{label}}</label>
    <input
        type={{type}}
        name={{name}}
        value={{value}}
        placeholder={{placeholder}}
    />
    <span class='error-{{name}}'>Неверный {{name}}</span>
</div>