const chat = `
<div class="chat-wrapper">
    <div class="chat">
        <div class="chat__inner">
            {{{leftPanel}}}
            <div class="chat-main">
                {{{chatHeader}}}
                {{{chatContent}}}
                {{{newMessage}}}
            </div>
        </div>
    </div>
</div>`;

export { chat };
