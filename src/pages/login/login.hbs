<div class="login__content">
    {{#> FormAuth name="login"}}
        <div class="login__content__header">
            <span>Вход</span>
            {{> Input name="login" }}
            {{> Input name="password" }}
        </div>
        <div class="login__content__footer">
            {{> Button name="signIn"}}
            {{> Button name="signUp"}}
        </div>
    {{/FormAuth}}
</div>
