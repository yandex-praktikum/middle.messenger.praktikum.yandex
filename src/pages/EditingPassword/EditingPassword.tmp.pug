.modal
  .modal__inner
    button.modal__close(title="Close modal")
    .modal__content
      section.user-settings
        .user-settings__f
          .user-settings__avatar(style=`background-image: url(${baseURL}resources${avatar})`)
          h2.user-settings__name= first_name

        .user-settings__s!= userSettings
        .user-settings__t
          | !{saveBtn}
          | !{linkToEditingSettings}
          | !{linkToSettings}
          | !{linkToChats}
