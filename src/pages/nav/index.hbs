{{!< base }}

<div class="nav">
    <h1 class="nav__title">
        Навигация
    </h1>
    <ul class="nav__list">
        {{#each nav }}
            {{> "nav/item" }}
        {{/each }}
    </ul>
</div>
