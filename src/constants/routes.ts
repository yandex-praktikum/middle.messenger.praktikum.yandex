export enum routes {
  empty = '',
  login = '#login',
  register = '#register',
  main = '#main',
  profile = '#profile',
  editUserdata = '#editUserdata',
  editPassword = '#editPassword',
  notFound = '#notFound',
  serverError = '#serverError',
}
