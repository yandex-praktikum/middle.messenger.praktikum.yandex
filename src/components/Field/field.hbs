<div class="string">
  <span class="label-profile">{{ label }}</span>
  {{#if isInput}}
    <input type="{{ type }}" name="{{ name }}" id="{{ name }}" placeholder="{{ placeholder }}" value="{{ value }}"
      class="profile-input {{ className }}">
    <span class="profile-input-error">{{ error }}</span>
  {{/if}}
  {{#if isData}}
    <span class="profile-data">{{ data }}</span>
  {{/if}}
</div>
