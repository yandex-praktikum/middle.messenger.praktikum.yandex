<div class="chat-item {{#if select}}select{{/if}}">
    <div class="group-avatar">
        {{#each avatars}}
            {{> Avatar }}
        {{/each}}
    </div>
    <div class="chat-item__content">
        <span>{{title}}</span>
        <div class="chat-item__content__last-message">
            {{#if lastMessage.personName}}
                <div class="chat-item__content__last-message-name">
                    {{ lastMessage.personName }}:
                </div>
            {{/if}}
            <div class="chat-item__content__last-message-text">
                {{ lastMessage.text }}
            </div>
        </div>
    </div>
    <div class="chat-item__status">
        <div class="chat-item__time">
            {{ lastMessage.time }}
        </div>
        {{#if numUnreadMessages}}
            <div class="chat-item__unread-message">
                {{ numUnreadMessages }}
            </div>
        {{/if}}
    </div>
</div>